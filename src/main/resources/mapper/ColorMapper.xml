<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.asagao.Mapper.ColorMapper">

	<resultMap id="ColorResultMap"
		type="com.asagao.Domain.Color">
		<id column="id" property="id" />
		<result column="name" property="name" />
	</resultMap>

	<select id="findAll" resultMap="ColorResultMap">
		SELECT
		c.id,
		c.name
		FROM
		color c
		ORDER BY c.id
	</select>
	
</mapper>
